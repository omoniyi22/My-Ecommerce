import axios from "axios";

// Limited results per search.
const fetchAPI = ({ search, minPrice, maxPrice }) => {

    if (minPrice && maxPrice)
        skip
    if (search) {
        queryproducts = product.filter()
    }
}